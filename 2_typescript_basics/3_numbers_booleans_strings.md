all types in typescript are lowercase so string, number

```typescript
function add(n1: number, n2: number, showResult: boolean, phrase: string) {
  if (showResult) {
    return phrase + (n1 + n2);
  }
}
```
